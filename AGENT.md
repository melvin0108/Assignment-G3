- ✅ **Silver layer** (`pipeline/silver/`) — implemented: 25 ordered Bronze-to-Silver table notebooks (`01_`–`28_`, including the defects-manifest mirror and two governance registries), `snapshot.py` helpers, and `silver_all_tables.py`. The runner validates that all inputs share one latest Bronze batch before executing the dependency-safe order. Silver produces current-state (Type 1) tables; it does not preserve SCD2 history.
